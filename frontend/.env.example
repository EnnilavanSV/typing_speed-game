# Only needed in production. Leave unset locally — graphql.ts falls back
# to http://localhost:4000/graphql automatically when this is undefined.
VITE_GRAPHQL_ENDPOINT="https://your-backend.onrender.com/graphql"
